# 🧾 سیستم حسابرسی هوشمند

یک پروژه **هوش مصنوعی + React + Django** برای بررسی اسناد مالی اتحادیه‌ها.  
هنگامی که اتحادیه‌ها اسناد خود را آپلود می‌کنند، هوش مصنوعی بررسی می‌کند و اگر مبلغ بیش از سقف تصویب شده باشد، سیستم می‌تواند اتصال مستقیم به **سامانه جامع مالیاتی (سامانه جام)** برقرار کند.

---

## ⚡ ویژگی‌ها
- **فرانت‌اند React** با رابط کاربری مدرن و واکنشگرا
- **بک‌اند Django** برای پردازش داده و منطق هوش مصنوعی
- بررسی خودکار اسناد مالی اتحادیه‌ها
- سیستم امن و قابل توسعه
- قابلیت گسترش برای اتصال به سامانه‌های دولتی

---

## 🛠️ نصب و اجرا (Development)

### بک‌اند (Django)
```bash
cd backend/afrashomal
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
cd backend/frontend
npm install
npm start
